{
  "name": "kickflow",
  "version": "0.4.0",
  "private": true,
  "description": "Personal-use Chrome MV3 extension for Kick.com: chat ban/delete message preservation and player quality-of-life.",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
