node_modules/
dist/
*.log
.cx_dispatch/
.ax_dispatch/
.claude/_dispatch/
council_mokick-lite/
.playwright-mcp/

# 7/24 monitor server — never commit data or secrets
server/node_modules/
server/data/
server/.env
*.jsonl
*.sqlite
*.sqlite3
*.db

# Playwright test output — throwaway Chromium profiles (contain real Cookies/Login Data/Web
# Data SQLite files), screenshots, harness bundles. Never commit browser profile state.
output/

# Internal dev-only working notes — kept LOCAL, never published (not user-facing).
STATUS.md
docs/*
!docs/screenshots/
docs/screenshots/*
!docs/screenshots/*.png
*.zip

# SECRETS - canonical list, synced-to C:/Users/ydbil/.claude/CLAUDE.md 2026-08-19
# Added because this repo's list was abbreviated while other carriers called it "never
# abbreviated". agy_snapshots/ is one of the two patterns that actually leaked on 2026-06-19.
cred_blob_*.bin
cred_blob*
snapshot*_*.json
agy_snapshots/
cookies_*.txt
*_cookies.txt
*.cred
token*.json
.env
.env.*
*secret*
# carve-out: source files legitimately carry "Secret" in their names in this tree
!*.cs
!*.py
!*.ts
!*.js
!*.md
